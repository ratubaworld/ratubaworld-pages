ONE STEP FOR YOU:

1) Double‑click DEPLOY.cmd (or PowerShell — run .\DEPLOY.ps1 from this folder).

2) When asked: paste one GitHub personal access token.
   Classic token with "repo" scope: https://github.com/settings/tokens/new

Everything else — new repos if needed, index.html REPO rewrite, git push, Pages on — runs from the script.

When the browser opens, install Utterances on YOUR "ratubaworld-comments" repo (GitHub forces this approval once).
